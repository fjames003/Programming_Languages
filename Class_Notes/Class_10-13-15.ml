(* Notes: *)

(* 
	Midterm = 55 points, max score = 50 -> 5 free points
		= Partial Credit Given
		Topics:
			-Everything up to today (up to types)
			-OCaml (recursion, higher-order functions, user-definded types)
			-Pattern Matching
			-Map, Filter, Folds
			-Syntax, Scope, Typing
			-Paradigms 
*)
(* --------------------------------------------------------------------- *)

(* 
	Exapmple: heterogeneous lists.
	List of int or bool
		-Python allows this
*)
 (* def negate1(e):
     if type(e) == int:
             return -e
     else:
             return not e
	*)